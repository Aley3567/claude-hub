# 设计参考

这里保存可公开的交互原型。HTML 原型使用合成渠道名，可提交到 GitHub。

以 `local-private-` 开头的截图只保留在桌面工作目录，可能包含像素化的真实渠道
信息，已由仓库 `.gitignore` 排除，禁止强制添加或上传。需要共享截图时，应先
制作一份所有渠道名、上游地址、余额和账号信息均已打码的新副本，再用不含
`local-private-` 的文件名保存。
